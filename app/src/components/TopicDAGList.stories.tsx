import type { Meta } from '@storybook/react'
import { TopicDAGList } from './TopicDAGList'

const meta: Meta<typeof TopicDAGList> = {
  title: 'TopicDAGList',
  component: TopicDAGList,
}
export default meta

export const Default = {}
